import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Toaster } from '@/components/ui/toaster';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'UISEK - Universidad Internacional SEK Ecuador',
  description: 'Universidad Internacional SEK Ecuador - Formando profesionales con excelencia académica y valores humanos. Oferta de pregrado, posgrado y educación continua.',
  openGraph: {
    title: 'UISEK - Universidad Internacional SEK Ecuador',
    description: 'Formando profesionales con excelencia académica y valores humanos.',
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
  twitter: {
    card: 'summary_large_image',
    images: [
      {
        url: 'https://bolt.new/static/og_default.png',
      },
    ],
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
